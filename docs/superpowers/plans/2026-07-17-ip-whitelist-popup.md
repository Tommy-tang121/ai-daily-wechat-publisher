# IP 白名单失败提示实施计划

> **状态：已完成。** 修复提交为 `feef30f`；2026-07-22 已通过微信 token 预检、真实 Windows 定时任务和官方草稿接口验证。

1. 在 `app/tests/test_cli.py` 增加测试：微信 `40164` 错误转换为包含出口 IP 的安全提示；定时任务保留工作流的失败原因。
2. 在 `app/tests/test_workflow.py` 增加测试：发布器遇到 `40164` 时，工作流把安全提示写入运行状态。
3. 先运行新增测试，确认旧行为无法通过验收。
4. 在 `app/src/ai_daily/daily_run.py` 集中处理失败原因的脱敏与 `40164` IP 提示。
5. 在 `app/src/ai_daily/cli.py` 复用该处理逻辑，并把已保存的发布失败原因传给 Windows 弹窗。
6. 运行新增测试、完整测试、`git diff --check`，检查变更不含密钥。
7. 合并至主目录、重启本地服务并触发一次 Windows 定时任务，报告实际结果。
